[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "atlas-forge"
version = "2.0.0"
requires-python = ">=3.11"
dependencies = ["fastapi","pydantic","starlette"]
